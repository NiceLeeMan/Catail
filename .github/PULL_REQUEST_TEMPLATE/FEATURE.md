<!-- 신규 기능 추가 전용. 구조 개선/코드 정리 목적이면 REFACTOR.md 사용 -->

## ❓ 작업을 하게된 배경

<!--
아래 순서로 작성한다 (4~5줄 내외):
1. 어떤 상위 문서(개념정의/기능명세/데이터명세/이슈)에서 이 작업이 파생됐는지 먼저 밝힌다.
2. 그 문서 안에서 왜 "지금 이 범위"를 처리해야 했는지 — 어떤 문제/누락/다음 단계 필요성이 트리거였는지 적는다.
3. 이 작업이 없으면 어떤 상태로 막혀있었는지(선행 조건 미충족 등)를 한 줄로 적는다.
4. 결론(무엇을 하기로 했는지)은 여기서 밝히지 않는다 — 그건 "결정 사항"에서 다룬다. 여기는 오직 "왜 이걸 지금 다뤄야 했는가"만 쓴다.
-->



## 🎯 결정 사항
<!--
이번 작업 중 여러 옵션 사이에서 실제로 판단이 필요했던 지점을 시간 순으로 기록한다.
각 항목은 "옵션 → 근거 → 선택"의 흐름을 유지한다.

- 결정 사항이 3개 이하면 아래 형식으로 직접 작성한다.
- 4개 이상이면 이 자리에는 요약만 남기고, 전체 내용은 별도 의사결정 문서로 분리해 링크한다.
-->

### 결정 1
- 옵션:
- 근거:
- 선택:

### 결정 2
- 옵션:
- 근거:
- 선택:

### 결정 3
- 옵션:
- 근거:
- 선택:

<!-- 4개 이상인 경우 -->
전체 결정 로그: [문서 링크]


## ⏳ 미정 사항
<!--
이번 작업 범위에서 판단을 미룬 항목을 기록한다. 각 항목은 다음을 포함한다.
1. 무엇을 보류했는지 (구체적으로 — "나중에 처리" 같은 표현 금지)
2. 왜 지금 결정하지 않았는지 (정보 부족 / 다른 도메인 결정에 종속 / 우선순위상 후순위 등)
3. 언제 다시 다룰 것인지 (다음 마일스톤명, 특정 이슈 번호, 또는 "다음 도메인 분석 시" 등 구체적 시점)
4. 방치될 경우 무엇이 깨지는지 한 줄 (없으면 "없음"으로 명시)
-->

| 보류 항목 | 보류 이유 | 재검토 시점 |
|---|---|---|
| | | |

해당 없으면: 없음


## ✅ 체크리스트
- [ ] 로컬에서 정상 동작하는가
- [ ] 관련 테스트가 모두 통과하는가
- [ ] Red 테스트를 본인이 직접 작성했는가
- [ ] 참조 문서(기능명세/데이터명세)와 실제 구현이 일치하는가
- [ ] 이번 PR에서 발생한 결정 사항이 위 "결정 사항"/의사결정 문서에 반영됐는가


## 🌐 연관 도메인 영향
<!--
이번 변경이 다른 도메인(FK로 얽힌 테이블, 공유 API, 다른 도메인의 정책 문서 등)에
영향을 주는 경우에만 작성한다. 영향이 없으면 "없음"으로 명시한다.

각 항목은 다음을 포함한다:
1. 영향을 받는 도메인/테이블/API가 무엇인지
2. 어떤 방식으로 영향을 주는지 (스키마 변경, 응답 필드 변경, 정책 변경 등)
3. 그 도메인 쪽에서 추가로 확인/대응해야 할 것이 있는지
-->

| 영향 받는 대상 | 영향 내용 | 추가 확인/대응 필요 사항 |
|---|---|---|
| | | |

해당 없으면: 없음


## 🔗 연관 이슈
closes #